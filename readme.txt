git clone <git地址>

提交命令：
	git add .
	git status 
	git commit -m '描述项目'

创建分支：
	git branch <分支名>

查看所有分支：
	git branch

切换分支:
	git checkout '切换到的分支'

将本地的分支版本上传到远程并合并:
	git push

推向指定的分支：
	git push --set-upstream origin <分支名>


eosjkp7236@sandbox.com
